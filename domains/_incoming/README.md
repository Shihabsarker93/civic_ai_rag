# Incoming Domain Materials

This folder is for domain source files that are available but not yet active in the production RAG pipeline.

Current active domain:

```text
domains/passport
```

When a new domain is ready, create a feature branch and move its files into a proper domain folder:

```bash
git checkout -b feature/add-brta-domain
```
